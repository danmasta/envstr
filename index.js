import { Envstr, factory, fromJSON, fromObject, fromPairs, fromTable } from './lib/envstr.js';

const envstr = factory();

export {
    Envstr,
    envstr,
    factory,
    fromJSON,
    fromObject,
    fromPairs,
    fromTable
};

export default envstr;
